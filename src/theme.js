import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#000080",
      light: "#ADD8E6",
    },
    secondary: {
      main: "#FFD700",
    },
    badge: {
      main: "red",
    },
  },
});
